fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
langchain-core==0.3.86
langchain-google-genai==2.1.12
google-genai==0.3.0
elevenlabs==1.9.0
httpx==0.27.2
pymongo==4.9.2
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.9.0
python-multipart==0.0.12
google-auth==2.35.0
cloudinary==1.41.0
# razorpay==1.4.2 imports pkg_resources unconditionally at module load time —
# pkg_resources ships inside setuptools, which recent Python/venv setups no
# longer install by default. Without this line, `import razorpay` itself
# fails with "No module named 'pkg_resources'" even before any Razorpay
# call is made. Pinned <81 because setuptools 81+ removed pkg_resources
# entirely (it's deprecated upstream, but razorpay 1.4.2 still needs it).
setuptools<81
razorpay==1.4.2